apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  externalTrafficPolicy: Local
  type: LoadBalancer
  #type: NodePort
  selector:
    app: nginx
  ports:
  - targetPort: 80
    port: 32142
    name: http
